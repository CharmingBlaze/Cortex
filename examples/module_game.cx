// Uses module-qualified calls: math.add, math.mul (emitted as math__add, math__mul).
// Compile with: cortex -i module_math.cx -i module_game.cx -o game

void main() {
    int x = math__add(1, 2);
    int y = math__mul(3, 4);
    show("math.add(1, 2) = " + x);
    show("math.mul(3, 4) = " + y);
}
