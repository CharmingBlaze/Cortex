// Test recursion with forward declaration
int factorial(int n);

void main() {
    printf("factorial(5)=%d\n", factorial(5));
}

int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
}
